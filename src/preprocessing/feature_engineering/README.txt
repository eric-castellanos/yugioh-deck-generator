Feature Engineering:

TODO:

1. Need to use some Data Versioning tool to keep track of different experimentation datasets.
2. Need to use MLFlow to keep track of clustering methods with TF-IDF only, Embeddings only, and TF_IDF + Embeddings.
3. Need to use MLFlow to keep track of differnet clustering methods, K-Means, UMAP, HBSCAN