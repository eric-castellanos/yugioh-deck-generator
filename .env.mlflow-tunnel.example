# Example environment configuration for MLflow with AWS backend
# Copy this file to .env.mlflow-tunnel and fill in your actual values

# Replace with your actual RDS endpoint
RDS_ENDPOINT=your-rds-endpoint.region.rds.amazonaws.com:5432
# Replace with your actual RDS password
MLFLOW_DB_PASSWORD=your-db-password
# Replace with your actual S3 bucket name
S3_BUCKET=your-s3-bucket-name

# AWS credentials - these will be set automatically by the mlflow-local.sh script
# when assuming the MLflow role or you can set them manually for direct access
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_SESSION_TOKEN=your-session-token
